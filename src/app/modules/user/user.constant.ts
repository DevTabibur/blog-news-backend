export enum USER_ROLE {
  SUPER_ADMIN = 'super admin',
  USER = 'user',
  ADMIN = 'admin',
}

export enum USER_STATUS {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  RETIRED = 'retired',
  BLOCKED = 'blocked',
}
